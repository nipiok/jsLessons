'use strict';

{
   var borderLeft = 3; /* переменная создана до того как была обявлена, 
   но значение не присвоено.
   Видна за пределами фигруных скобок */
}

let secound = 2; /* Переменная будет присвоено по факту выполнения
строки в коде. Не видна за пределами фигурных скобок */

const pi = 3.14; /* Нельзя поменять, в остальном аналог let */